# ===========================================
# GreenScape Lawn Care Frontend Configuration
# ===========================================

# API Configuration
# Point this to your backend API URL
# For local development: http://localhost:5000/api
# For production: https://api.greenscape.com/api
VITE_API_URL=http://localhost:5000/api

# Environment
# Options: development, staging, production
VITE_APP_ENV=production

# Google Maps API
# Get your key from: https://console.cloud.google.com/google/maps-apis/credentials
# Enable the following APIs: Maps JavaScript API, Places API, Geocoding API
# NOTE: This should be the same key used in the backend .env file
VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# ===========================================
# Build-time Environment Variables
# ===========================================
# When building with Docker, these variables are embedded into the build
# They cannot be changed after the build is complete
# To change them, rebuild the Docker image with new values
